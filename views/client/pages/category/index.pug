extends ../../layout/default.pug
include ../../mixins/box-head.pug 
include ../../mixins/product-layout.pug 

block content 
    .section-products-featured.my-5 
        .container 
            .row 
                .col-12 
                    +box-head(`${categorys.title}`)
            +product-layout(product)