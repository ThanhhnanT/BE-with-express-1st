mixin search(keyword)
    form(id="form-search")
         .input-group   
            input(
                type="text"
                placeholder="Nhập từ khóa"
                name="keyboard"
                class="form-control"
                value = keyword
            )
            .input-grourp-append 
                button(
                    class="btn btn-success"
                    type="submit"
                ) Tìm
                            